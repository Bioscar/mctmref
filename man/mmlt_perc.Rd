% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentiles.R
\name{mmlt_perc}
\alias{mmlt_perc}
\title{Marginal Conditional Percentile Curves from an MCTM}
\usage{
mmlt_perc(
  m_full,
  m1,
  m2,
  marginal,
  newdata,
  tau,
  plot = FALSE,
  n_grid = 10000L,
  ...
)
}
\arguments{
\item{m_full}{A fitted \code{mmlt} object.}

\item{m1}{Fitted marginal \code{mlt} model for the first response.}

\item{m2}{Fitted marginal \code{mlt} model for the second response.}

\item{marginal}{Integer, 1 or 2. Which marginal to compute.}

\item{newdata}{A \code{data.frame} of covariate values. Each row gives one
covariate value at which percentile curves are evaluated.}

\item{tau}{Numeric vector of probability levels in (0, 1).}

\item{plot}{Logical. If \code{TRUE}, adds lines to the current plot.
If \code{FALSE} (default), returns a \code{data.frame}.}

\item{n_grid}{Integer. Response grid size for numerical inversion.
Default 10000.}

\item{...}{Additional graphical arguments passed to \code{lines()} when
\code{plot = TRUE}.}
}
\value{
If \code{plot = FALSE}: a \code{data.frame} with the covariate
  column(s) from \code{newdata} followed by one column per \code{tau} level.
}
\description{
Numerically inverts the marginal CDF \eqn{F_j(y_j \mid x)} from the joint
\code{mmlt} model to obtain conditional quantiles \eqn{Q_\tau(Y_j \mid x)}.
}
\details{
Uses \code{predict(m_full, margins = j, type = "distribution")} on a dense
response grid, then finds the value closest to each requested \eqn{\tau}.
}
\examples{
\dontrun{
nd  <- data.frame(age = seq(20, 85, length.out = 50))
pct <- mmlt_perc(fit$m_full, fit$m1, fit$m2,
                 marginal = 1, newdata = nd,
                 tau = c(0.025, 0.5, 0.975))
plot(df$age, df$fpg, col = "grey80", pch = 16)
lines(pct$age, pct[["0.5"]], lwd = 2)
}
}
\references{
Lado-Baleato et al. (2023). \doi{10.1002/bimj.202200229}
}
\seealso{
\code{\link{mctm_fit}}, \code{\link{bivreg_mctm}}
}
