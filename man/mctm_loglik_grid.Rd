% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglikelihood.R
\name{mctm_loglik_grid}
\alias{mctm_loglik_grid}
\title{Joint Log-Likelihood on a Response Grid}
\usage{
mctm_loglik_grid(
  m_full,
  x_val,
  x_name = "age",
  y1_grid = NULL,
  y2_grid = NULL,
  n_grid = 50L,
  m1 = NULL,
  m2 = NULL
)
}
\arguments{
\item{m_full}{A fitted \code{mmlt} object.}

\item{x_val}{Numeric scalar. Covariate value.}

\item{x_name}{Character. Covariate variable name. Default \code{"age"}.}

\item{y1_grid}{Numeric vector. Grid for \eqn{y_1}. If \code{NULL}, derived
from \code{mkgrid(m1)}.}

\item{y2_grid}{Numeric vector. Grid for \eqn{y_2}. If \code{NULL}, derived
from \code{mkgrid(m2)}.}

\item{n_grid}{Integer. Grid size when \code{y1_grid}/\code{y2_grid} are
auto-generated. Default 50.}

\item{m1}{Fitted marginal \code{mlt} model for \eqn{y_1} (needed for
auto grid range).}

\item{m2}{Fitted marginal \code{mlt} model for \eqn{y_2}.}
}
\value{
A \code{data.frame} with \eqn{y_1}, \eqn{y_2}, the covariate,
  \code{joint_density}, and \code{joint_loglik}.
}
\description{
Evaluates \eqn{f(y_1, y_2 \mid x)} on a grid of \eqn{(y_1, y_2)} values
at a fixed covariate value. Useful for density contour plots.
}
\examples{
\dontrun{
g <- mctm_loglik_grid(fit$m_full, x_val = 50, m1 = fit$m1, m2 = fit$m2)
fpg_vals   <- sort(unique(g$fpg))
hba1c_vals <- sort(unique(g$hba1c))
contour(fpg_vals, hba1c_vals,
        matrix(g$joint_density, nrow = length(fpg_vals)))
}

}
\seealso{
\code{\link{mctm_loglik}}, \code{\link{mctm_loglik_by_age}}
}
