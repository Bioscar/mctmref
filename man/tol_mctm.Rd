% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tolerance_regions.R
\name{tol_mctm}
\alias{tol_mctm}
\title{Bivariate Conditional Tolerance Region from an MCTM}
\usage{
tol_mctm(
  m_full,
  m1,
  m2,
  newdata,
  train_data = NULL,
  tau = 0.95,
  alpha = 0.05,
  B = 1000L,
  n_grid = 2000L
)
}
\arguments{
\item{m_full}{A fitted \code{mmlt} object.}

\item{m1}{Fitted marginal \code{mlt} model for the first response.}

\item{m2}{Fitted marginal \code{mlt} model for the second response.}

\item{newdata}{A \code{data.frame} of covariate values. One region per row.}

\item{train_data}{Optional \code{data.frame} used to fit the model. If
\code{NULL} (default), the data stored inside \code{m_full} is used.}

\item{tau}{Single numeric coverage level in (0, 1). Default 0.95.}

\item{alpha}{Uncertainty level. Default 0.05 (95\% confidence).}

\item{B}{Integer. Monte Carlo iterations for tolerance factor. Default 1000.}

\item{n_grid}{Integer. Response grid size. Default 2000.}
}
\value{
A list with one \eqn{50 \times 2} numeric matrix per row of
  \code{newdata}, giving the tolerance region boundary in
  \eqn{(y_1, y_2)} space.
}
\description{
Estimates a conditional tolerance region that contains \eqn{100\tau\%} of
the reference population with confidence \eqn{1-\alpha}, following
Krishnamoorthy & Mondal (2006).
}
\details{
The tolerance factor is estimated by Monte Carlo following Krishnamoorthy &
Mondal (2006). The tolerance ellipse is built from the empirical mean and
covariance of the training Z-scores, then back-transformed to response
space. The resulting region is always larger than the corresponding
reference region from \code{\link{bivreg_mctm}}.
}
\examples{
\dontrun{
tol <- tol_mctm(fit$m_full, fit$m1, fit$m2,
                newdata = data.frame(age = 50))
lines(tol[[1]], col = "navy", lty = 2, lwd = 2)
}

}
\references{
Krishnamoorthy, K. and Mondal, S. (2006). Improved tolerance factors for
multivariate normal distributions. \emph{Communications in Statistics:
Simulation and Computation}, 35(2), 461--478.

Lado-Baleato et al. (2023). \doi{10.1002/bimj.202200229}
}
\seealso{
\code{\link{bivreg_mctm}}, \code{\link{mctm_fit}}
}
