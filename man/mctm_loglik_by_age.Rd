% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglikelihood.R
\name{mctm_loglik_by_age}
\alias{mctm_loglik_by_age}
\title{Joint Log-Likelihood as a Function of the Covariate}
\usage{
mctm_loglik_by_age(
  m_full,
  y1_val,
  y2_val,
  x_seq = seq(20, 85, by = 0.5),
  x_name = "age",
  m1 = NULL,
  m2 = NULL
)
}
\arguments{
\item{m_full}{A fitted \code{mmlt} object.}

\item{y1_val}{Numeric. Observed value of the first response.}

\item{y2_val}{Numeric. Observed value of the second response.}

\item{x_seq}{Numeric vector. Covariate values to evaluate.
Default \code{seq(20, 85, by = 0.5)}.}

\item{x_name}{Character. Covariate name. Default \code{"age"}.}

\item{m1}{Fitted marginal \code{mlt} model (to get \eqn{y_1} name).}

\item{m2}{Fitted marginal \code{mlt} model (to get \eqn{y_2} name).}
}
\value{
A \code{data.frame} with columns \code{x}, \code{joint_density},
  \code{joint_loglik}, and \code{most_likely} (logical).
}
\description{
For a patient with known \eqn{(y_1, y_2)}, evaluates
\eqn{\log f(y_1, y_2 \mid x)} across a range of covariate values.
The maximising value is the covariate level at which the patient's
marker combination is most typical.
}
\examples{
\dontrun{
prof <- mctm_loglik_by_age(fit$m_full, y1_val = 95, y2_val = 5.5,
                            m1 = fit$m1, m2 = fit$m2)
plot(prof$x, prof$joint_loglik, type = "l",
     xlab = "Age", ylab = "log f(fpg, hba1c | age)")
abline(v = prof$x[prof$most_likely], lty = 2)
}

}
\seealso{
\code{\link{mctm_loglik}}, \code{\link{mctm_loglik_grid}}
}
