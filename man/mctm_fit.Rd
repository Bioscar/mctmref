% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{mctm_fit}
\alias{mctm_fit}
\title{Fit a Bivariate Multivariate Conditional Transformation Model}
\usage{
mctm_fit(
  data,
  y1,
  y2,
  x,
  bounds_y1 = range(data[[y1]]),
  bounds_y2 = range(data[[y2]]),
  bounds_x = range(data[[x]]),
  order_y = 6L,
  order_x = 6L,
  support_prob = c(0.05, 0.95)
)
}
\arguments{
\item{data}{A \code{data.frame} with response and covariate columns.}

\item{y1}{Character. Name of the first response (e.g. \code{"fpg"}).}

\item{y2}{Character. Name of the second response (e.g. \code{"hba1c"}).}

\item{x}{Character. Name of the continuous covariate (e.g. \code{"age"}).}

\item{bounds_y1}{Numeric length-2 vector. Hard bounds for \code{y1}'s
Bernstein basis. Default \code{range(data[[y1]])}.}

\item{bounds_y2}{Numeric length-2 vector. Hard bounds for \code{y2}'s
Bernstein basis. Default \code{range(data[[y2]])}.}

\item{bounds_x}{Numeric length-2 vector. Hard bounds for the covariate
basis. Default \code{range(data[[x]])}.}

\item{order_y}{Integer. Bernstein polynomial order for response bases.
Default 6 (recommended by Klein et al. 2022).}

\item{order_x}{Integer. Bernstein polynomial order for covariate basis.
Default 6.}

\item{support_prob}{Numeric length-2 vector. Quantile probabilities defining
the inner support of each Bernstein basis. Default \code{c(0.05, 0.95)}.}
}
\value{
An object of class \code{mctm_fit}, a list with elements:
\describe{
  \item{m1}{Fitted marginal \code{mlt} model for \code{y1}.}
  \item{m2}{Fitted marginal \code{mlt} model for \code{y2}.}
  \item{m_full}{Fitted joint \code{mmlt} model.}
  \item{data}{Data used for fitting.}
  \item{y1, y2, x}{Variable names.}
}
}
\description{
Convenience wrapper that defines Bernstein basis functions and fits the
two marginal \code{mlt} models and the joint \code{mmlt} model in one call.
}
\examples{
\dontrun{
library(refreg)
df  <- subset(refreg::aegis, dm == "no")
fit <- mctm_fit(df, y1 = "fpg", y2 = "hba1c", x = "age",
                bounds_y1 = c(64, 183), bounds_y2 = c(4.3, 7.8),
                bounds_x  = c(19, 89))
print(fit)
}

}
\references{
Lado-Baleato et al. (2023). Multivariate reference and tolerance regions
based on conditional transformation models. \emph{Biometrical Journal},
65, 2200229. \doi{10.1002/bimj.202200229}
}
\seealso{
\code{\link{bivreg_mctm}}, \code{\link{tol_mctm}},
  \code{\link{mmlt_perc}}
}
